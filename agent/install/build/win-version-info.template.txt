# Windows VERSIONINFO resource for the agent .exe.
# Consumed by PyInstaller's --version-file flag. The build script substitutes
# {VERSION_TUPLE} (e.g. "(0, 2, 4, 0)") and {VERSION_STRING} ("0.2.4.0")
# from the current git tag before passing it to PyInstaller.
#
# Real metadata lifts the binary out of "no signature, no metadata" territory
# which Windows Defender's ML model treats as a strong malware signal.

VSVersionInfo(
  ffi=FixedFileInfo(
    filevers={VERSION_TUPLE},
    prodvers={VERSION_TUPLE},
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0)
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          u'040904B0',
          [
            StringStruct(u'CompanyName', u'RemoteConnect'),
            StringStruct(u'FileDescription', u'RemoteConnect Remote Support Agent'),
            StringStruct(u'FileVersion', u'{VERSION_STRING}'),
            StringStruct(u'InternalName', u'RemoteConnectAgent'),
            StringStruct(u'LegalCopyright', u'© 2026 RemoteConnect. All rights reserved.'),
            StringStruct(u'OriginalFilename', u'RemoteConnectAgent-win.exe'),
            StringStruct(u'ProductName', u'RemoteConnect'),
            StringStruct(u'ProductVersion', u'{VERSION_STRING}'),
            StringStruct(u'Comments', u'Authorized remote support agent. Source: https://github.com/BlAcQW/Remoteconnect'),
          ]
        )
      ]
    ),
    VarFileInfo([VarStruct(u'Translation', [1033, 1200])])
  ]
)
