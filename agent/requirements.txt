websockets==12.0
httpx==0.27.0
mss==9.0.1
Pillow>=10.4.0,<12
pynput==1.7.6
pyperclip==1.9.0
python-dotenv==1.0.1
# daily-python was removed in v0.2.0. Video is now streamed as MJPEG over
# the agent's existing WebSocket (see agent.agent.stream_loop). The legacy
# Daily.co subprocess path in agent.publisher_daily is still callable when
# the backend explicitly requests it (RC_VIDEO_BACKEND=daily) but is no
# longer pulled in as a default dependency — it had no Windows wheels.
